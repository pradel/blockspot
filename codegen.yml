schema: http://localhost:3000/graphql
overwrite: true
generates:
  ./server/graphql/types/resolvers.ts:
    config:
      contextType: ./context#GraphqlContext
    plugins:
      - typescript-common
      - typescript-server
      - typescript-resolvers
